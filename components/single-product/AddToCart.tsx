import { Button } from '../ui/button';

const AddToCart = ({ productsId }: { productId: string }) => {
  return <Button className="capitalize mt-8 size='lg">Add to Cart</Button>;
};
export default AddToCart;
